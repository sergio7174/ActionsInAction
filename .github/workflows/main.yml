    name: My First Workflow

    on:
     push:
         branches:
             - main
     workflow_dispatch:

     Jobs:
       MyFirstJob:
         runs-on: ubuntu-latest
         steps:
           - run: echo "👋 Hello World!"
           - name: Cache
  uses: actions/cache@v5.0.4
   
